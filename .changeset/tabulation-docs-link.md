---
"witify-skills": patch
---

`frontend-development` no longer copies the `tabulation` class list; it links to `https://ui.sprintify.app/llm/tabulation.txt`, now published by sprintify-ui, and adds the rule for tables at the bottom of a `BaseCard` (`tabulation-border-b-0`). `sprintify-ui` lists the new doc next to the component index.
